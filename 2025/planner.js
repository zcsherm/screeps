class Planner{
    
    constructor(room, room_level=0){
        this.goals = [];
        this.home_room = room;
        this.level = level;
        this.energy_max = 0;
        this.current_energy = 0;
    };

    initialize{
        /*
        On initializing, we should find our spawn, find the sources, find the controller
        */
    };
    first_level_planner(){
        /*
        *Goals at RCL one include:
        * Upgrade to RCL 2
        * Plan where to start building Extensions and roads
        */
    };
};
